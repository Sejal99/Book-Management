/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['s3-alpha-sig.figma.com', 'static.vecteezy.com'], // Add any other domains you need here
    },
  };
  
  export default nextConfig;
  